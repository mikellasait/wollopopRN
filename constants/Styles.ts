import Colors from "./Colors";
import { StyleSheet } from "react-native";

export const defaultStyles = StyleSheet.create({
  btn: {
    backgroundColor: Colors.wollapop,
    borderRadius: 20,
    justifyContent: "center",
    alignItems: "center",
    height: 40,
  },
});
